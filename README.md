# HAVING - nos permite filtrar depois que uma agregação já ocorreu.

#### 1. Síntaxe básica
````
SELECT column, SUM(column)
FROM table
WHERE column != ‘Google’
GROUP BY column
````
#### Para mais exemplos, verificar o arquivo sql
